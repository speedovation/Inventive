[class*="col-"]
    padding-right: (gutterWidth / 2)
    float: left

[class*="col-"]:last-child
    padding-right 0    
	
generate-grid("col")

generate-offset("col") 

generate-pull("col")
generate-push("col")
